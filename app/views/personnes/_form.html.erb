<%= form_for(personne) do |f| %>
  <% if personne.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personne.errors.count, "error") %> prohibited this personne from being saved:</h2>

      <ul>
      <% personne.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %>
    <%= f.text_field :nom %>
  </div>

  <div class="field">
    <%= f.label :prenom %>
    <%= f.text_field :prenom %>
  </div>

  <div class="field">
    <%= f.label :date_naissance %>
    <%= f.date_select :date_naissance %>
  </div>

  <div class="field">
    <%= f.label :voyage_ids %>
    <%= f.collection_select :voyage_ids, Voyage.all, :id, :voyage,  {}, {:multiple => true}  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
