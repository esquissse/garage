<%= form_for(voiture) do |f| %>
  <% if voiture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voiture.errors.count, "error") %> prohibited this voiture from being saved:</h2>

      <ul>
      <% voiture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :immatriculation %>
    <%= f.text_field :immatriculation %>
  </div>

  <div class="field">
    <%= f.label :couleur %>
    <%= f.text_field :couleur %>
  </div>

  <div class="field">
    <%= f.label :modele %>
    <%= f.text_field :modele %>
  </div>

  <div class="field">
    <%= f.label :date_mise_en_service %>
    <%= f.date_select :date_mise_en_service %>
  </div>

  <div class="field">
    <%= f.label :marque %>
    <%= f.collection_select :marque_id, Marque.order(:libelle).all, :id, :libelle  %>
  </div>

  <div class="field">
    <%= f.label :personne %>
    <%= f.collection_select :personne_id, Personne.order(:nom).all , :id, :nom_complet  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
